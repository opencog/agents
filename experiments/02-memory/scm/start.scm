#! /usr/bin/env guile
-s
!#
;
; start.scm -- startup script
;
(use-modules (opencog) (opencog sensory) (opencog cogserver))
(use-modules (opencog persist) (opencog persist-rocks))

; Placeholder -- nothing here yet.
